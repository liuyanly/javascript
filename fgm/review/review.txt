-------------------------------------------2017-5-27-------------------------------------------
2-12、封装一个函数 检查全选   更新状态(全选或者全不选)
     全选/全不选
     反选
     根据复选框个数更新全选框状态
3-9、封装一个函数，获得给定的时间，
         时间小于等于0时，清除定时器，
         自减1(--)
         实时更新倒计后的值，
        分除以60(1分等于10秒)
        秒 %=60（秒取余） 
      
       封装函数不足两位前补0
       
       点击启动按钮执行
       判断按钮的className为空，执行自减函数，否则清除定时器
       点击按钮判断，className 为空加上className否则置为空

出错总结
1、三目运算符 条件语句一定是==
