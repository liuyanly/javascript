-------------------------------------------2017-5-27-------------------------------------------
4-1、
1、鼠标移动到1级菜单显示2级菜单 
2、鼠标移动切换时需要clearTimeout取消由setTimeout()方法设置的timeout
3、1级菜单的宽度减去2级菜单offseLeft 大于当2级菜单offseWidth? 靠左显示 : 否则靠右显示
4、小箭头需要注意一下,需要用当前li的offsetLeft减去2级菜单的offsetLeft+li的宽度-箭头的宽度 除以2 （当2级菜单居右显示时就能体现出这样做的效果了）
5、阻止事件冒泡
6、应用setTimeout指定毫秒数后调用函数或者计算表达式

4-2、
1、封装函数 传参 显示当前参数的大图
添加淡入淡出效果 所有大图设置opacity=0
setInterval指定间隔秒数不停增加opacity达到淡入淡出效果,透明度为100时clearInterval
2、数字切换应用封装的函数
3、自动播放函数
setIeterval间隔2秒
参数自加,大于等于大图长度,重置参数为0,
4、鼠标滑过关闭定时器
5、鼠标离开自动播放定时器

4-3、
自动播放顺序
设置一个变量为boolean
为真index++
为假index--
当index>=图片的长度，设置index为图片长度-2，
当index<=0 设置index=0

4-6、Form事件
onfocus 当元素获得焦点时运行的脚本
onblur 元素失去焦点时运行的脚本

4-7、
innerHTML: 也就是从对象的起始位置的全部内容，包括HTML标签。
innerText: 从起始位置到终止位置的内容，但它去除HTML标签
inner.outHTML:除了包含innerHTML的全部内容外，还包含对象标签本身。
特别说明：innerHTML是符合W3C标准的属性，而innerText只适用于IE浏览器，因此，尽可能地去使用innerHTML,而少用innerText,如果要输出不含HTML标签的内容，可以使用innerHTML取得包含HTML标签的内容后，再用正则表达式去除HTML标签

4-8、
数组方法
删除第一项 shift()
添加第一项unshift()
删除最后一项pop()方法
添加最后一项push()方法
join() 方法用于把数组中的所有元素放入一个字符串
concat()方法 用于连接两个或者多个数组。
该方法不会改变现有的数组，而仅仅会返回被连接数组的一个副本
splice()方法 向/人数组中添加/删除项目,然后返回被删除的项目。
请注意：splice()方法与slice()方法的作用是不同的，splice()方法会对数组进行修改
slice()方法可从已有的数组中返回选定的元素
请注意，该方法并不会修改数组，而是返回一个子数组，如果想删除数组中的一段元素，应该使用方法Array.splice().
